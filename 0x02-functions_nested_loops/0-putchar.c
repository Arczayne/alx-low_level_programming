#include <stdio.h>
/**
 * main - prints the alphabets in lowercase,
 * followed by a new line
 * Return: Always 0 (Success)
 */
int main(void)
{
char ch;
#include "main.h"
putchar('\n');
return (0);
}
